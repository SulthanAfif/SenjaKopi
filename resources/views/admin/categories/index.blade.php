@extends('layouts.admin')
@section('content')
<div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-[#c08246]">Catalog</p><h1 class="text-3xl font-black">Kategori</h1></div><a href="{{ route('admin.categories.create') }}" class="sk-btn-primary">+ Tambah kategori</a></div>
<div class="admin-card mt-6 overflow-hidden"><div class="hidden grid-cols-[80px_1fr_1fr_100px_160px] border-b border-stone-200 bg-stone-50 px-5 py-3 text-xs font-bold uppercase tracking-wide text-stone-500 sm:grid"><div>ID</div><div>Nama</div><div>Slug</div><div>Produk</div><div class="text-right">Aksi</div></div>@foreach($categories as $category)<div class="grid gap-3 border-b border-stone-200 px-5 py-4 last:border-b-0 sm:grid-cols-[80px_1fr_1fr_100px_160px] sm:items-center"><div class="text-xs font-bold text-stone-400">#{{ $category->id }}</div><div class="font-bold">{{ $category->name }}</div><div class="text-sm text-stone-500">{{ $category->slug }}</div><div class="font-bold">{{ $category->products_count }}</div><div class="flex justify-end gap-2"><a href="{{ route('admin.categories.edit',$category) }}" class="sk-btn-soft px-3 py-2 text-xs">Edit</a><form action="{{ route('admin.categories.destroy',$category) }}" method="POST">@csrf @method('DELETE')<button onclick="return confirm('Hapus kategori ini?')" class="rounded-xl px-3 py-2 text-xs font-bold text-red-600 hover:bg-red-50">Hapus</button></form></div></div>@endforeach</div>
@endsection
