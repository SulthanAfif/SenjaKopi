@extends('layouts.admin')
@section('content')
<div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-xs font-bold uppercase tracking-[.18em] text-[#c08246]">Catalog</p><h1 class="text-3xl font-black">Produk</h1><p class="mt-1 text-sm text-stone-500">Kelola menu dan ubah ketersediaan tanpa harus masuk halaman edit.</p></div><a href="{{ route('admin.products.create') }}" class="sk-btn-primary">+ Tambah produk</a></div>
<form class="admin-card mt-6 grid gap-3 p-4 sm:grid-cols-2 xl:grid-cols-[1fr_180px_160px_auto_auto]" method="GET"><input class="sk-input" name="q" value="{{ request('q') }}" placeholder="Cari nama produk..."><select class="sk-input" name="category_id"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category_id')==$category->id)>{{ $category->name }}</option>@endforeach</select><select class="sk-input" name="availability"><option value="">Semua status</option><option value="1" @selected(request('availability')==='1')>Tersedia</option><option value="0" @selected(request('availability')==='0')>Tidak tersedia</option></select><button class="sk-btn-primary">Filter</button><a href="{{ route('admin.products.index') }}" class="sk-btn-soft">Reset</a></form>
<div class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">@forelse($products as $product)<article class="admin-card overflow-hidden"><div class="relative"><img src="{{ $product->image_url }}" class="h-48 w-full object-cover" alt="{{ $product->name }}"><span class="absolute right-3 top-3 rounded-full px-3 py-1 text-xs font-black shadow-sm {{ $product->is_available ? 'bg-emerald-50 text-emerald-700' : 'bg-red-50 text-red-700' }}">{{ $product->is_available ? 'Tersedia' : 'Habis' }}</span></div><div class="p-5"><span class="rounded-full bg-[#f6ede5] px-2.5 py-1 text-[10px] font-bold text-[#7a4b2d]">{{ $product->category->name }}</span><h2 class="mt-3 font-black">{{ $product->name }}</h2><p class="mt-1 text-sm text-stone-500">Rp {{ number_format($product->price,0,',','.') }}</p><div class="mt-4 grid grid-cols-2 gap-2"><form action="{{ route('admin.products.toggleAvailability',$product) }}" method="POST" class="col-span-2">@csrf @method('PATCH')<button class="w-full rounded-xl border px-4 py-2.5 text-sm font-bold {{ $product->is_available ? 'border-amber-200 text-amber-700 hover:bg-amber-50' : 'border-emerald-200 text-emerald-700 hover:bg-emerald-50' }}">{{ $product->is_available ? 'Tandai habis' : 'Aktifkan produk' }}</button></form><a href="{{ route('admin.products.edit',$product) }}" class="sk-btn-soft">Edit</a><form action="{{ route('admin.products.destroy',$product) }}" method="POST">@csrf @method('DELETE')<button onclick="return confirm('Hapus produk ini?')" class="w-full rounded-xl border border-red-200 px-4 py-2.5 text-sm font-bold text-red-600 hover:bg-red-50">Hapus</button></form></div></div></article>@empty<div class="admin-card col-span-full px-5 py-16 text-center text-sm text-stone-500">Produk tidak ditemukan.</div>@endforelse</div><div class="mt-6">{{ $products->links() }}</div>
@endsection
